# Local Database Configuration (PostgreSQL 17)
spring.datasource.url=jdbc:postgresql://localhost:5432/nexushr
spring.datasource.username=postgres
spring.datasource.password=postgres

# Aiven Cloud Database Configuration (Reference Only)
# spring.datasource.url=jdbc:postgresql://syncwork-sahilsepat2-559b.c.aivencloud.com:12835/defaultdb?sslmode=require
# spring.datasource.username=avnadmin
# spring.datasource.password=${DB_PASSWORD}

# --- Development Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Email Configuration (using Gmail with App Password) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=syncwork0@gmail.com
# THE FIX: The password will be read from an environment variable named GMAIL_APP_PASSWORD
spring.mail.password=${GMAIL_APP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- HTTP API Email Option ---
nexushr.brevo.api-key=${BREVO_API_KEY:}
